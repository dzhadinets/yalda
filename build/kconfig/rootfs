menu "RootFS"

config YALDA_ROOTFS_ADDONS
  string "Addons path"
  help
    Path to addons to rootfs with configs, binaries etc

config YALDA_ROOTFS_COREDUMPS
  bool "Enable core dumps for applications"
  default n
  help
    It creates /.init_core_dumps file

endmenu
